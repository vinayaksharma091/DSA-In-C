#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
     int size,value,i,flag=0;
    scanf("%d",&size);
    int arr[size];
    for(i=0;i<size;i++){
        scanf("%d",&arr[i]);
    }
    scanf("%d",&value);
     for (int i=0;i<size;i++){
         if(arr[i]== value)
         {
             flag =1;
             break ;
         }
     }
    if(flag==1){
        printf("Found");
    }
    else {
        printf("Not Found ");
    }
       return 0;
}
